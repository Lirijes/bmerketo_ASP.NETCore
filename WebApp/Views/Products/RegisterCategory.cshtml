@model ProductRegistrationViewModel
@{
    ViewData["Title"] = "Register a new product";
}

<div class="d-flex justify-content-center">
    <div class="col-6">
        <form method="post" novalidate>

            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            @*<div class="input-section">
                <label asp-for="Form.CategoryModel.Name" class="form-label"></label>
                <input asp-for="Form.CategoryModel.Name" class="form-control" />
                <small><span asp-validation-for="Form.CategoryModel.Name" class="text-danger"></span></small>
            </div>*@

            <div class="d-grid mt-2">
                <button type="submit" class="btn-theme">CREATE CATEGORY</button>
            </div>

        </form>
    </div>
</div>